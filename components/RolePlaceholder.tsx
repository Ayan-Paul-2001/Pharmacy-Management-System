import { RoleShell, RoleIntro, PortalCard } from '@/components/RoleShell'
export function RolePlaceholder({ role, title, description, cards }: { role: 'owner'|'employee'|'customer'; title: string; description: string; cards: [string,string,string,string][] }) { return <RoleShell role={role} title={title}><RoleIntro kicker={`${role} room · Workspace`} title={title} description={description} action="Create new"/><div className="portal-grid">{cards.map(c=><PortalCard key={c[0]} href={c[1]} icon={c[3]} title={c[0]} description={c[2]} metric="Ready to explore"/>)}</div></RoleShell> }
